<Window x:Class="ProyectoVerano2.Empleados.Lista"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProyectoVerano2.Empleados"
        mc:Ignorable="d"
        Title="Window1" Height="800" Width="600">
    <Grid>
        <DataGrid x:Name="dtgEmpleadpos" SelectionChanged="dtgEmpleadpos_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="ID" Header="ID" Binding="{Binding Path=ID}" Width="60" />
                <DataGridTextColumn x:Name="Nombre" Header="Nombre" Binding="{Binding Path=Nombre}" Width="120"/>
                <DataGridTextColumn x:Name="Apellido" Header="Apellido" Binding="{Binding Path=Apellido}" Width="160"/>
                <DataGridTextColumn x:Name="DNI" Header="DNI" Binding="{Binding Path=DNI}" Width="120"/>
                <DataGridTextColumn x:Name="TipoEmpeado" Header="TipoEmpleado" Binding="{Binding Path=TipoEmpleado}" Width="* "/>
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Window>
